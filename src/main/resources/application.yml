server:
  port: 8080
spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/covid?characterEncoding=UTF8&serverTimezone=Asia/Shanghai&useSSL=false
      username: root
      password: 123456
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    return-instance-for-empty-row: true
  type-aliases-package: edu.xhu.pojo
  mapper-locations: /mapper/*.xml

